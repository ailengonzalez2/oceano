export default defineAppConfig({
  ui: {
    // Keep any Nuxt UI chrome inside the ocean palette; the site is photo-first
    // so UI components stay deliberately minimal.
    colors: {
      primary: 'primary',
      neutral: 'slate'
    }
  }
})
